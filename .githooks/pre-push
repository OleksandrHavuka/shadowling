#!/bin/sh
# pre-push — run the full dev gate again before sharing, as a safety net in case a
# commit used `--no-verify` or was amended. Same check.sh as pre-commit.
# Activate once per clone:  git config core.hooksPath .githooks
exec "$(git rev-parse --show-toplevel)/plugins/shadowling/check.sh"
